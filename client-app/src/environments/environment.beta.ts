import { EnvironmentSetting } from "../types/app-setting.type";

export const environment: EnvironmentSetting = {
    appName: "reactivities-app",
    environment: "beta",
    apiUrl: "https://reactivities-beta.com"
}